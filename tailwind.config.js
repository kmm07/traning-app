/** @type {import('tailwindcss').Config} */

/*
 * ═══════════════════════════════════════════════════════════════════════════
 *  هويةُ لوحة المدرّب — مشتقّةٌ من أيقونة التطبيق لا مخترعة
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * 🎨 **المصدر مقيس**: `public/icon-512.png` في مستودع الخادم — شعارُ «PT»
 *    بلونٍ ليمونيّ على أسودَ شبهِ خالص. والتقطيعُ اللونيّ للأيقونة أعطى
 *    **`#090909` أرضيةً** و**`#D1FE0F` لوناً مميّزاً** — وهما ما يُبنى
 *    عليهما هذا الملفّ. و`theme-color` في `index.html` كان `#090909` سلفاً
 *    فالأرضيةُ متّفقةٌ مع ما يراه المستخدم قبل أن تُحمَّل الصفحة.
 *
 * ⛔ **والقديمُ كان بنفسجياً/أزرق** (`deep_purple-A200 #7551ff` وتدرّجات
 *    `#00a4f9 → #006bf2`) — بقايا قالبٍ مولَّد لا صلة له بالتطبيق. فمن يفتح
 *    اللوحة بعد التطبيق كان يظنّهما منتجين مختلفين.
 *
 * 🔑 **والقاعدةُ في هذا الملفّ: تُعاد قيمةُ الرمز القائم ولا يُعاد تسميته.**
 *    `gray-900_01` (٣١ موضعاً) و`blue_gray-900_01` (٢٨) و`blue_gray-900`
 *    (٢٩) و`shadow-bs` (٩) و`bg-gradient` (٨) — **مئةُ موضعٍ في الصفحات
 *    تتبدّل بتبديل قيمها هنا وحدها**، بلا لمسِ ملفِّ صفحةٍ واحد. وإعادةُ
 *    التسمية كانت تعني مسحاً على ١٥٥ ملفاً وفرصةَ خطإٍ في كلٍّ منها.
 *
 * ⚖️ **والأسماءُ القديمة تبقى كلُّها** — أيُّ صنفٍ يستعمله كودٌ قائم يبقى
 *    معرَّفاً. الحذفُ كان يُنتج أصنافاً صامتة (Tailwind لا يُخطئ على صنفٍ
 *    مجهول، بل **لا يُخرج شيئاً**) وهو أخبثُ عطلٍ في هذه الطبقة.
 */

/** الليمونيُّ — لونُ الشعار. سلّمٌ كامل كي لا تُخترع درجاتٌ في الصفحات. */
const brand = {
  50: "#FAFFE5",
  100: "#F2FFB8",
  200: "#E8FF7A",
  300: "#DCFF3D",
  400: "#D1FE0F", // ← اللونُ المقيس من الأيقونة بالضبط
  500: "#CFFF0F", // ← ما كان مسجَّلاً في `lime-100`؛ يُبقى مرادفاً
  600: "#B2DC00",
  700: "#8CAE00",
  800: "#5F7600",
  900: "#3A4800",
  950: "#1E2600",
};

/** الحياديُّ — من أسود الأيقونة صعوداً. أرضيةٌ وأسطحٌ وحدودٌ ونصوص. */
const ink = {
  50: "#F6F6F7",
  100: "#E9E9EC",
  200: "#C9C9CF",
  300: "#A1A1AA",
  400: "#7A7A83",
  500: "#57575E",
  600: "#3C3C42",
  700: "#2A2A2E", // حدٌّ بارز
  750: "#232326", // حدٌّ عاديّ
  800: "#1A1A1D", // سطحٌ مرتفع
  850: "#141416", // سطحٌ (بطاقة)
  900: "#0F0F11", // سطحٌ غائر
  950: "#090909", // الأرضية — لونُ الأيقونة
};

export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      screens: { md: { max: "1050px" }, sm: { max: "550px" } },

      /*
       * 🔴 **الخطُّ كان بلا حروفٍ عربية.** كان المستورد `DM Sans` — وهو
       *    لاتينيٌّ خالص لا يحمل محرفاً عربياً واحداً — واللوحةُ عربيةٌ
       *    بالكامل ⇒ كلُّ نصٍّ فيها كان يسقط على خطِّ النظام الافتراضيّ،
       *    فيختلف شكلُ اللوحة بين جهازٍ وجهاز ولا يتحكّم فيه أحد.
       *    و`font-roboto` في التخطيط كان يشير إلى عائلةٍ غير معرَّفة أصلاً.
       */
      fontFamily: {
        sans: [
          "IBM Plex Sans Arabic",
          "ui-sans-serif",
          "system-ui",
          "-apple-system",
          "Segoe UI",
          "Noto Sans Arabic",
          "Tahoma",
          "Arial",
          "sans-serif",
        ],
        // يبقى الاسمُ معرَّفاً لأن التخطيط كان يستعمله، فلا يصير صنفاً صامتاً.
        roboto: [
          "IBM Plex Sans Arabic",
          "ui-sans-serif",
          "system-ui",
          "sans-serif",
        ],
        /** للأرقام في البطاقات الإحصائية — أرقامٌ متساوية العرض لا ترقص. */
        numeric: ["ui-monospace", "SFMono-Regular", "Menlo", "monospace"],
      },

      colors: {
        brand,
        ink,

        /** أسماءٌ دلاليةٌ تُقرأ في الصفحة بمعناها لا بدرجتها. */
        surface: {
          DEFAULT: ink[850],
          raised: ink[800],
          sunken: ink[900],
          base: ink[950],
        },
        line: { DEFAULT: ink[750], strong: ink[700] },
        content: {
          DEFAULT: ink[50],
          muted: ink[300],
          faint: ink[400],
          inverse: ink[950],
        },
        success: {
          100: "#DCFCE7",
          200: "#4ADE80",
          400: "#37D67A",
          500: "#22C55E",
          600: "#16A34A",
          bg: "#0E2A1B",
        },
        danger: {
          100: "#FEE2E2",
          200: "#FCA5A5",
          400: "#FF6B6B",
          500: "#F04438",
          600: "#DC2626",
          bg: "#2A1214",
        },
        warning: {
          100: "#FEF3C7",
          200: "#FCD34D",
          400: "#FFB020",
          500: "#F59E0B",
          600: "#D97706",
          bg: "#2A2110",
        },
        /*
         * ألوانُ الماكروز — أربعُ فئاتٍ في حلقاتٍ متجاورة.
         *
         * ⛔ **كانت مكتوبةً نصّاً في `NutritionInfo`** بأربعة رموزٍ سداسية،
         *    ومنها `#E80054` **مكرَّرٌ في `TrainingInfo`** لحلقةِ تقدّمِ
         *    التدريب — أي أن «السعرات» و«تقدّم البرنامج» بلونٍ واحدٍ بلا
         *    صلةٍ بينهما، وتعديلُ أحدهما لا يمسّ الآخر (نمط مصدرَي حقيقة).
         *    والأربعةُ هنا نقطةُ قرارٍ واحدة، وكلٌّ منها مقروءٌ على الأسود.
         */
        macro: {
          calories: brand[400],
          protein: "#FFB020",
          carbs: "#38BDF8",
          fat: "#F472B6",
        },
        info: {
          100: "#DBEAFE",
          200: "#93C5FD",
          400: "#38BDF8",
          500: "#0EA5E9",
          600: "#0284C7",
          bg: "#0B2331",
        },

        /*
         * ── الأسماءُ القائمة: تُعاد قيمُها إلى الهوية وتبقى أسماؤها ──
         *
         * كلُّ مفتاحٍ أدناه مستعملٌ في صفحاتٍ قائمة. تبديلُ القيمة يرسم
         * الهويةَ الجديدة في تلك المواضع كلِّها بلا تعديلِ سطرٍ فيها.
         */
        gray: {
          400: ink[300],
          500: ink[400],
          800: ink[600],
          900: ink[900],
          "900_10": ink[800],
          "800_bf": "#2A2A2Ebf",
          "900_02": ink[950],
          "900_03": ink[850], // أرضيةُ النوافذ والأدراج
          "900_04": ink[850],
          "900_05": ink[800],
          "900_01": ink[950], // ← أرضيةُ اللوحة كلِّها (٣١ موضعاً)
          "900_06": ink[800],
          "900_07": ink[800],
          "900_08": ink[800], // ← خلفيةُ بند الشريط النشط
          "900_09": ink[950], // ← أرضيةُ صفحة الدخول
        },
        red: {
          400: "#FF6B6B",
          900: "#7A1D1D",
          A100_01: "#FCA5A5",
          "400_01": "#F04438",
          A100: "#FCA5A5",
        },
        light_blue: {
          500: brand[400],
          A700_6c: "#D1FE0F6c",
          A700: brand[600],
          A700_87: "#D1FE0F87",
        },
        /** `lime-100` كان اللونَ الصحيح سلفاً — يبقى مرادفاً للمميّز. */
        lime: { 100: brand[500], 400: brand[400], 500: brand[500] },
        teal: { 500: "#22C55E", A400_01: "#37D67A", A400: "#2DD4BF" },
        blue_gray: {
          100: ink[200],
          400: ink[300],
          900: ink[800], // ← أسطحُ القوائم المنسدلة (٢٩ موضعاً)
          "900_a5": "#1A1A1Da5",
          "900_02": ink[800],
          "900_01": ink[750], // ← لونُ الحدود (٢٨ موضعاً)
          "900_04": ink[750],
          "900_03": ink[800],
          "100_60": "#C9C9CF60",
        },
        /** البنفسجيّ كان «الأساسيَّ» — يصير الليمونيّ فيتبعه كلُّ مستعمليه. */
        deep_purple: {
          A400: brand[600],
          A200: brand[400], // ← `Button primary` و`Input` وغيرهما
          A100: brand[300],
          A700: brand[700],
        },
        blue: { 500: brand[400] },
        indigo: { 200: ink[300], 300: ink[400], 500: brand[600] },
        cyan: { A400: "#38BDF8" },
        amber: { 400: "#FFB020", A400: "#FFB020" },
        black: { 900: "#000000" },
        pink: { A400: "#F04438" },
        /** كان يُستعمل بلا تعريف (`text-white-A700`) فلا يُخرج شيئاً. */
        white: { A700: "#FFFFFF", A700_33: "#FFFFFF33" },
        /*
         * أسماءٌ كانت مستعملةً في الأصناف **وغيرَ معرَّفةٍ إطلاقاً** —
         * `!bg-secondary-100` و`!text-dark-100` و`!text-error-200`
         * و`!text-success-200` و`!border-error-100`. كانت تُكتب ولا تُخرج
         * لوناً، فيبدو الشرطُ منفَّذاً والشاشةُ لا تفرّق بين حالتين.
         */
        secondary: { 100: ink[800], 200: ink[700] },
        dark: { 100: ink[950], 200: ink[900] },
        error: { 100: "#F04438", 200: "#FF6B6B" },
      },

      borderRadius: {
        card: "20px",
        field: "14px",
        pill: "999px",
      },

      boxShadow: {
        /*
         * 🔴 **كان ظلاً نيومورفياً مزدوجاً** (`6px 6px 12px #121120,
         *    -5px -5px 12px #232239, inset ...`) — يرسم حوافَّ فاتحةً
         *    وغامقةً حول كل بطاقة فتبدو الشاشةُ محفورةً، وهو أسلوبٌ يجعل
         *    الحدودَ تتنازع مع الحدِّ الحقيقيّ للبطاقة فيُقرأ الاثنان خطّين.
         *    صار ظلاً واحداً ناعماً يرفع البطاقة عن الأرضية بلا ضجيج.
         */
        bs: "0 1px 2px 0 rgb(0 0 0 / 0.40), 0 8px 24px -12px rgb(0 0 0 / 0.60)",
        card: "0 1px 2px 0 rgb(0 0 0 / 0.40), 0 8px 24px -12px rgb(0 0 0 / 0.60)",
        raised:
          "0 2px 4px 0 rgb(0 0 0 / 0.45), 0 16px 40px -16px rgb(0 0 0 / 0.70)",
        pop: "0 8px 16px -4px rgb(0 0 0 / 0.50), 0 24px 56px -20px rgb(0 0 0 / 0.75)",
        /** توهّجُ التركيز — يستعمله الزرُّ المميّز والحقلُ عند التركيز. */
        glow: "0 0 0 1px rgb(209 254 15 / 0.35), 0 8px 28px -10px rgb(209 254 15 / 0.30)",
        /*
         * ➕ **ظلُّ الرفع عند المرور** — درجةٌ واحدة فوق `card`، فالبطاقةُ
         *    القابلة للنقر ترتفع قليلاً بدل أن تبدّل لونَها وحده. والفرقُ
         *    بينهما محسوسٌ ولا يُقرأ وميضاً.
         */
        lift: "0 4px 10px -2px rgb(0 0 0 / 0.45), 0 20px 44px -18px rgb(0 0 0 / 0.65)",
        /** ظلُّ الزرِّ المميّز في سكونه — يفصله عن السطح بلا توهّج. */
        btn: "0 1px 2px 0 rgb(0 0 0 / 0.30), 0 6px 16px -8px rgb(209 254 15 / 0.35)",
        /** حافّةٌ داخليةٌ فاتحةٌ في الأعلى — تُعطي الأسطحَ الداكنة حجماً. */
        hairline: "inset 0 1px 0 0 rgb(255 255 255 / 0.05)",
      },

      backgroundImage: {
        /** كان أزرق (`#00a4f9 → #006bf2`) ⇒ صار تدرّجَ الشعار. */
        gradient: `linear-gradient(135deg, ${brand[300]}, ${brand[500]})`,
        gradient1: `linear-gradient(135deg, ${ink[800]}, ${ink[850]})`,
        gradient2: `linear-gradient(135deg, ${brand[400]}, ${brand[600]})`,
        gradient3: `radial-gradient(${ink[800]}, ${ink[900]})`,
        gradient4: `linear-gradient(135deg, ${brand[300]}, ${brand[500]})`,
        gradient5: `linear-gradient(135deg, ${brand[400]}, ${brand[600]})`,
        /** وهجٌ خفيفٌ خلف الشعار وبطاقاتِ الرأس. */
        "brand-veil":
          "radial-gradient(60% 80% at 70% 0%, rgb(209 254 15 / 0.10), transparent 70%)",
        /** لمعةٌ علويةٌ خفيفةٌ على الأسطح — تفصل البطاقة عن الأرضية بلا حدّ ثانٍ. */
        sheen:
          "linear-gradient(to bottom, rgb(255 255 255 / 0.04), transparent 40%)",
        /** أرضيةُ بند الشريط النشط — تدرّجٌ يخفت باتّجاه الحافّة. */
        "nav-active":
          "linear-gradient(to left, rgb(209 254 15 / 0.16), rgb(209 254 15 / 0.04))",
        /** أرضيةُ الزرِّ المميّز — ليمونيٌّ بعمقٍ خفيفٍ بدل مسطّحٍ تامّ. */
        "brand-btn": `linear-gradient(180deg, ${brand[300]}, ${brand[400]} 55%, ${brand[500]})`,
      },

      /*
       * ═══════════════════════════════════════════════════════════════════
       *  الحركة — منحنىً واحدٌ لكل اللوحة
       * ═══════════════════════════════════════════════════════════════════
       *
       * ⚖️ **والقاعدة: منحنىً واحد وثلاثةُ أزمنة، لا رقمٌ يُخترع في كل
       *    مكوّن.** `ease-smooth` يبدأ سريعاً ويستقرّ بهدوء — وهو ما يجعل
       *    الفتحَ والانتقالَ يُقرآن «سلسين» بدل أن يُقرآ بطيئين. والأزمنة
       *    ثلاثة: **120ms** للمسِّ (ضغطةُ زرّ) · **200ms** للحالة (مرورٌ ·
       *    لونٌ · طيّ) · **320ms** للدخول (نافذةٌ · درجٌ · صفحة).
       */
      transitionTimingFunction: {
        smooth: "cubic-bezier(0.16, 1, 0.3, 1)",
        snap: "cubic-bezier(0.3, 0, 0.2, 1)",
      },
      transitionDuration: { 120: "120ms", 320: "320ms" },

      /*
       * 🔴 **وكلُّ حركةِ دخولٍ تنتهي إلى `transform: none` لا إلى المطابقة
       *    الصفرية — والفرقُ هو الفرقُ بين لوحةٍ تعمل ولوحةٍ لا تعمل.**
       *
       * الأربعُ تُشغَّل بـ`fill-mode: both` فتُبقي **آخرَ إطارٍ** مسنَداً
       * إلى الأبد. و`translateY(0)` تحويلٌ قائمٌ لا غيابُ تحويل ⇒ العنصرُ
       * يصير (١) **الكتلةَ الحاوية لكلّ `position: fixed` بداخله** و(٢)
       * **سياقَ تكديسٍ** يحبس `z-index` أبنائه.
       *
       * وأثرُه مقيسٌ على `<main>` التي تحمل `animate-page-in`: درجُ daisyUI
       * `position: fixed; top: 0` وكذلك النافذة `z-index: 999` ⇒
       *   · الدرجُ يُوضَع عند **رأس الصفحة** لا رأس الشاشة، فمن ضغط صفّاً
       *     في أسفل جدولٍ طويل يجد الدرجَ فوقه ويلزمه أن يمرّر ليراه؛
       *   · و`z-50`/`z-999` محبوسان داخل `main` فيعلوهما **الرأسُ اللاصق**
       *     (`z-40`) وهو أخوها لا ابنُها ⇒ يغطّي أعلى الدرج.
       *
       * ⚖️ و`none` تنتهي إليها الحركةُ نفسُها بلا فارقٍ بصريّ (المطابقةُ
       * الصفرية والغيابُ يرسمان الشيء عينه)، فالعلاجُ كلمةٌ لا إعادةُ بناء.
       */
      keyframes: {
        "fade-in": {
          from: { opacity: "0", transform: "translateY(4px)" },
          to: { opacity: "1", transform: "none" },
        },
        "slide-in": {
          from: { opacity: "0", transform: "translateX(8px)" },
          to: { opacity: "1", transform: "none" },
        },
        /** دخولُ الصفحة — ارتفاعٌ خفيفٌ مع تلاشٍ، لا انزلاقٌ يُشتّت. */
        "page-in": {
          from: { opacity: "0", transform: "translateY(8px)" },
          to: { opacity: "1", transform: "none" },
        },
        /** دخولُ النوافذ — تكبيرٌ خفيفٌ من ٩٦٪ فتبدو قادمةً من العمق. */
        "scale-in": {
          from: { opacity: "0", transform: "scale(0.96) translateY(8px)" },
          to: { opacity: "1", transform: "none" },
        },
        shimmer: {
          "100%": { transform: "translateX(-100%)" },
        },
      },
      animation: {
        "fade-in": "fade-in 220ms cubic-bezier(0.16,1,0.3,1) both",
        "slide-in": "slide-in 220ms cubic-bezier(0.16,1,0.3,1) both",
        "page-in": "page-in 320ms cubic-bezier(0.16,1,0.3,1) both",
        "scale-in": "scale-in 220ms cubic-bezier(0.16,1,0.3,1) both",
      },
    },
  },

  // eslint-disable-next-line no-undef
  plugins: [require("tailwindcss-rtl"), require("daisyui")],

  /*
   * 🔴 **وdaisyUI كان على سمته الافتراضية.** كلُّ `btn-primary` و
   *    `badge-warning` و`bg-base-100` و`modal` و`drawer` في اللوحة كانت
   *    تأخذ ألوانَ السمة الجاهزة (بنفسجيٌّ وأزرق وأبيض) — لا ألوانَ
   *    التطبيق. وهي مستعملةٌ في النوافذ والأدراج والقوائم المنسدلة
   *    والشارات، أي في كل شاشةٍ تقريباً. فتُعرَّف سمةٌ واحدة باسم اللوحة.
   */
  daisyui: {
    themes: [
      {
        pt: {
          primary: brand[400],
          "primary-content": ink[950],
          secondary: ink[800],
          "secondary-content": ink[50],
          accent: brand[500],
          "accent-content": ink[950],
          neutral: ink[800],
          "neutral-content": ink[50],
          "base-100": ink[850],
          "base-200": ink[900],
          "base-300": ink[950],
          "base-content": ink[50],
          info: "#38BDF8",
          "info-content": ink[950],
          success: "#37D67A",
          "success-content": ink[950],
          warning: "#FFB020",
          "warning-content": ink[950],
          error: "#F04438",
          "error-content": "#FFFFFF",
          "--rounded-box": "20px",
          "--rounded-btn": "14px",
          "--rounded-badge": "999px",
          "--animation-btn": "0.2s",
          "--border-btn": "1px",
        },
      },
    ],
    darkTheme: "pt",
  },
};
